<!DOCTYPE html>
<html>
  <head>
    <title>Mini MessageBoard</title>
    <link rel="stylesheet" href="/styles.css" />
    <%- include("./header"); -%>
  </head>
  <body>
    <div class="container">
      <ul>
        <% if (messages.length > 0) { %> <% messages.forEach((message) => { %>
        <li>
          <p><strong><%= message.username %></strong></p>
          <p><%= message.content %></p>
          <p><small><%= new Date(message.added).toLocaleString() %></small></p>
          <a href="/messages/<%= message.id %>" class="button">Open</a>
        </li>
        <% }) %> <% } else { %>
        <li>No messages found.</li>
        <% } %>
      </ul>
      <a href="/new" class="button">Add a new message</a>
    </div>
  </body>
  <footer><%- include("./footer"); -%></footer>
</html>
